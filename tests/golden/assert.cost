dot              work ≤ a.len()                    moves ≤ 16·a.len()                 declared
                 inferred         work 6·a.len()                    moves 16·a.len() + 2·B             within the declaration
                 lower bound      moves 16·a.len()                   (footprint, every distinct element crosses once)   gap 1× at M = 2 MiB, B = 64
                 footprint        a: [0, 8·a.len())  b: [0, 8·a.len())   resident after if 16·a.len() < M
pairs            work 6·a.len()² + 2·a.len()       moves                              exact  (2 regimes)
                                               moves 16·a.len() + 2·B             if ≈ 8·a.len() < M
                                               moves 8·a.len()² + 2·B·a.len()     if ≈ 8·a.len() ≥ M
                 lower bound      moves 8·a.len()                    (footprint, every distinct element crosses once)   gap 2× if ≈ 8·a.len() < M at M = 2 MiB, B = 64
                 lower bound      moves 16·a.len()²/M − M            (HBL, σ = 2, CDKSY 2013)   gap 1048576× if ≈ 8·a.len() ≥ M at M = 2 MiB, B = 64
                 footprint        a: [0, 8·a.len())   resident after if 8·a.len() < M
main             work 73                           moves 2·B + 120                    exact, io
                 rests on         dot (declared, checked)
