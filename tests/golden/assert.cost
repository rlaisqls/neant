dot              work 6·a.len()                    moves 16·a.len()                   exact
pairs            work 6·a.len()² + 2·a.len()       moves 8·a.len()² + B·a.len()       exact
main             work 83                           moves 4·B + 40                     exact, io
