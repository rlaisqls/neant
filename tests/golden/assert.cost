dot              work 6·a.len() + 3                moves 16·a.len()                   exact
pairs            work 6·a.len()² + 2·a.len() + 2   moves 8·a.len()² + B·a.len()       exact
main             work 86                           moves 4·B + 40                     exact, io
