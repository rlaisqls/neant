#[cost(moves_at_most = "8 a.len()")]
fn pairs(a: &[i64]) -> i64 {
    let mut s = 0;
    for i in 0..a.len() {
        for j in 0..a.len() {
            s += a[i] * a[j];
        }
    }
    s
}
fn main() { let y = [1, 2, 3]; println(pairs(&y)); }
