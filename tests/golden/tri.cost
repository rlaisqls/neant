pairs            work 3·a.len()² + 5·a.len()       moves                              exact  (2 regimes)
                                               moves 4·a.len()² + 2·B·a.len() + 4·a.len() if ≈ 8·a.len() ≥ M
                                               moves ≈ 4·a.len()²                 if ≈ 8·a.len() < M
tiles            work 21·n/4 + 1                   moves 8·n + 2·B                    exact
main             work 102                          moves 8·B + 200                    exact, io
