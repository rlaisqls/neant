is_space         work 5                            moves 0                            exact
count_words      work 16·s.len()                   moves s.len() + B                  exact
                 lower bound      moves s.len()                      (footprint, every distinct element crosses once)   gap 1× at M = 2 MiB, B = 64
                 footprint        s: [0, s.len())   resident after if s.len() < M
upcase           work 12·s.len()                   moves 2·s.len() + 2·B              exact
                 lower bound      moves s.len()                      (footprint, every distinct element crosses once)   gap 2× at M = 2 MiB, B = 64
                 footprint        s: [0, s.len())   resident after if s.len() < M
is_palindrome    work 7·s.len() − 4                moves 2·B·s.len() − 2·B            exact
                 footprint        s: [0, s.len()) (whole array)   no residue claimed
main             work 1024                         moves 30·B + 108                   exact, io
