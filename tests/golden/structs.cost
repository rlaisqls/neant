norm2            work 5                            moves 0                            exact
sum_x            work 4·ps.len()                   moves 24·ps.len() + B              exact
                 lower bound      moves 8·ps.len()                   (footprint, every distinct element crosses once)   gap 3× at M = 2 MiB, B = 64
                 footprint        ps: [0, 24·ps.len() − 16)   resident after if ≈ 24·ps.len() < M
sum_all          work 8·ps.len()                   moves 24·ps.len() + B              exact
                 lower bound      moves 24·ps.len()                  (footprint, every distinct element crosses once)   gap 1× at M = 2 MiB, B = 64
                 footprint        ps: [0, 24·ps.len())   resident after if 24·ps.len() < M
shift            work 5·ps.len()                   moves 24·ps.len() + B              exact
                 lower bound      moves 8·ps.len()                   (footprint, every distinct element crosses once)   gap 3× at M = 2 MiB, B = 64
                 footprint        ps: [0, 24·ps.len() − 16)   resident after if ≈ 24·ps.len() < M
main             work 91                           moves 4·B + 224                    exact, io
